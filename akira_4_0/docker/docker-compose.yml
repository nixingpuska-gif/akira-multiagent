# AKIRA 4.0 Docker Compose
# Auto-generated by AKIRA Self-Generation API
# Generated: 2026-01-27T17:29:39.743780

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: akira-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgresql:
    image: postgres:15-alpine
    container_name: akira-postgres
    environment:
      POSTGRES_DB: akira_db
      POSTGRES_USER: akira
      POSTGRES_PASSWORD: akira_secure_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U akira"]
      interval: 10s
      timeout: 5s
      retries: 5

  akira-core:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: akira-core
    depends_on:
      redis:
        condition: service_healthy
      postgresql:
        condition: service_healthy
    environment:
      REDIS_HOST: redis
      POSTGRES_HOST: postgresql
      POSTGRES_USER: akira
      POSTGRES_PASSWORD: akira_secure_password
      AKIRA_MODE: production
    ports:
      - "8000:8000"
      - "8001:8001"
      - "8002:8002"
    volumes:
      - ./akira_4_0:/app/akira_4_0
      - ./logs:/app/logs
    restart: unless-stopped

volumes:
  redis_data:
  postgres_data:

networks:
  default:
    name: akira-network
